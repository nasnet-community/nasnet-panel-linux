- Add router mode with multi-WAN failover, VPN pools, Wi-Fi, LAN devices, port forwarding, and live traffic diagnostics.
- Improve inbound/outbound editors, mobile layouts, terminal sessions, and dashboard performance.
- Fix installation and updates, TLS validation, traffic accounting, and Xray reverse tunnels.

**Breaking changes:** Legacy database migrations and remote-agent support are removed. Existing databases need current link keys, VPN pools, VLESS reverse rules, and non-null upload/download counters. Subscription API clients must switch to `page`/`per_page`.

Back up the database and configuration before upgrading.

[Full changelog](https://github.com/nasnet-community/nasnet-panel-linux/compare/v1.0.0...v2.0.0)
